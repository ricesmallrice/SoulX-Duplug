# SoulX-Duplug 对话历史记录

## 项目简介

SoulX-Duplug 是一个插拔式流式语义 VAD 模型，用于实时全双工语音对话系统。核心功能：
- **传统 VAD**：基于音频能量检测语音活动
- **语义 VAD**：判断语义内容
- **全双工语音交互**：用户和系统可同时听和说
- **WebSocket 通信**：客户端与服务器之间实时音频流传输

## 系统架构

```
浏览器前端 ──WebSocket──> 对话系统(app.py, 55556) ──> VAD服务(server.py, 8000)
                                            └──> LLM服务(llm_server.py, 6007)
                                            └──> TTS服务(api_server.py, 6006)
```

## 服务启动命令

```bash
# VAD 服务（端口 8000）
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug
source .venv/bin/activate
CUDA_VISIBLE_DEVICES=0 python server.py

# 对话系统（端口 55556）
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug/dialogue_system
source /nfs/kubeflow-data/SoulX/SoulX-Duplug/.venv/bin/activate
python app.py

# LLM 服务（端口 6007）
python dialogue_system/modules/qwen_llm/llm_server.py

# TTS 服务（端口 6006）
python dialogue_system/modules/index_tts_vllm/api_server.py
```

---

## 已解决的问题

### 1. 环境配置与 GPU 资源问题

**问题**：CUDA 显存不足（out of memory）

**解决**：
- 为每个服务指定 GPU：`CUDA_VISIBLE_DEVICES=<GPU编号> python xxx.py`
- 发现训练容器 `ppgqwen` 占用 GPU，用 `docker stop ppgqwen` 停止

**问题**：虚拟环境激活报错

**解决**：使用正确路径：
```bash
source /nfs/kubeflow-data/SoulX/SoulX-Duplug/.venv/bin/activate
```

### 2. 远程访问问题

**问题**：无法访问服务器桌面 / 访问 localhost:55556 空白页

**解决**：使用 SSH 隧道转发：
```bash
ssh -L 55556:localhost:55556 hjadmin@172.88.88.14
```
然后通过 Chrome 访问 `http://localhost:55556`。

### 3. ASR 模块替换

**问题**：需要更换 ASR 模块

**解决**：
- 在 `model/asr.py` 中新增 `WhisperASR` 类，支持 Whisper 模型
- 在 `config/config.yaml` 中配置 ASR 模型
- 后来用户决定使用 SenseVoice，恢复 `config.yaml` 配置：
  ```yaml
  asr:
    # model_name: paraformer
    model_name: sensevoice
    language: auto
  ```

### 4. 代码对比与修改验证

**问题**：对比 `/nfs/kubeflow-data/SoulX/SoulX-Duplug`（修改版）和 `SoulX-Duplug-orgin`（原始版）的代码差异

**分析结果**：
| 文件 | 差异 | 结论 |
|------|------|------|
| `config/config.yaml` | ASR 模型从 paraformer 改为 sensevoice | 配置变更，无问题 |
| `service/model.py` | 新增 Qwen7B_Client、计时日志 | 增强功能 |
| `server.py` | 新增 turn_log 事件 | 增强功能 |
| `model/asr.py` | 新增 WhisperASR 类 | 增强功能 |
| `dialogue_system/app.py` | 修复了 vad_loading 时序、interrupt 锁、backchannel | 已修复 |

### 5. 修复的代码问题

**问题 1：`vad_loading` 发送时序错误**

`vad_loading` 状态在 session 创建前发送，`emit_to_room` 找不到 session 会直接返回，导致前端收不到 loading 状态。
**修复**：删除了 session 创建前发送的 `vad_loading`，移到 session 创建后。

**问题 2：interrupt/reset_interrupt 无锁保护**

存在竞态条件，中断信号可能在设置后立即被重置。
**修复**：用 `with session.lock` 包裹 interrupt/reset_interrupt 操作。

**问题 3：Backchannel 检测被注释掉**

所有语音片段（包括"嗯""啊"等附和词）都会触发完整 LLM+TTS 流水线。
**修复**：取消注释 `check_backchannel` 检测逻辑，恢复"嗯""啊"时跳过 LLM+TTS。

**问题 4：`vad_client.py` 超时太短（1 秒）**

VAD 服务处理包含 VAD + ASR + Qwen7B 调用，耗时超过 1 秒，客户端 `ws.recv()` 超时断连。
**修复**：超时从 `1.0` 改为 `30.0` 秒。后因用户恢复原代码，改回 `1.0` 秒。

### 6. WebSocket 断连问题排查

**现象**：前端浏览器显示：
```
[WebSocket] connected
Connection confirmed
TTS Audio Engine Initialized
[WebSocket] disconnected
```
页面一直显示 "No transcription yet"。

**排查过程**：
1. 确认 VAD 服务（8000）、对话系统（55556）是否在运行
2. 查看 VAD 服务日志，发现 `[TurnTaking] websocket connected` 后又 `disconnected`
3. 原因是 VAD 服务处理（VAD + ASR + Qwen7B LLM 调用）耗时超过客户端 1 秒超时
4. 或者 VAD 服务没启动导致 `ConnectionRefusedError: [Errno 111]`

**解决**：确认所有服务都启动后正常。

### 7. 分支切换

**问题**：`SoulX-Duplug-orgin` 要切换到 `dialogue-system` 分支

**解决**：
```bash
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug-orgin
git switch dialogue-system
```
当前 commit：`66a9f74`

### 8. 完整恢复代码到 orgin 版本

**问题**：`SoulX-Duplug` 修改过太多文件，想完全恢复

**解决**：
```bash
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug
git restore config/config.yaml model/asr.py server.py service/model.py
rm requirements_no_tensorrt.txt
```
恢复后 `git status` 干净，`.modelscope_cache/` 是模型缓存不影响。

### 9. 模型下载问题

**问题**：VAD 服务启动时下载 Paraformer 模型（859MB），不想等待

**解决**：
- 杀掉下载进程
- 确认 `config.yaml` 使用 `sensevoice`（已下载好的模型）

---

## 关键概念说明

### 全双工 vs 半双工
- 全双工：用户和系统可同时听和说，实时交互
- 半双工：一次只能一方说话

### WebSocket 消息 type 类型
- `turn_state`：VAD 状态返回（idle/nonidle/speak）
- `connect_ack`：连接确认
- `vad_loading`：VAD 加载状态
- `user_transcription`：用户语音识别文本
- `resume_audio`：恢复音频播放
- 等

### 状态返回频率
- 每次音频 chunk 处理完成后返回一次状态

---

## 遗留注意事项

1. **服务启动顺序**：先启动 VAD 服务（8000）→ LLM（6007）→ TTS（6006）→ 对话系统（55556）
2. **改配置后重启**：只有 VAD 服务（server.py）读取 `config/config.yaml`，改完只需重启它
3. **GPU 分配**：每个服务用不同 GPU（`CUDA_VISIBLE_DEVICES`）避免显存不足
4. **远程访问**：需要通过 SSH 隧道访问 55556 端口
