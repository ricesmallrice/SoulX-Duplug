# SoulX-Duplug 对话历史整理

> 本文件整理了在 SoulX-Duplug 项目上遇到的所有问题及解决方案。

---

## 一、已解决的问题汇总

### 1. 代码覆盖与一致性
- **问题**：`SoulX-Duplug` 目录中的代码被修改过，需要还原
- **解决**：使用 `SoulX-Duplug-orgin` 中的原代码覆盖被修改的部分
- **最终差异**：仅保留 7 个文件的必要修改（时延计时、配置调优、中文提示词）

### 2. 大模型文件完整性检查
- **问题**：担心大模型文件损坏
- **解决**：确认所有模型文件大小与原始目录一致，没有损坏，并清理了临时文件

### 3. 时延测试系统
- **问题**：需要了解并优化整个对话流程的时延
- **解决**：在程序中添加了完整的时延测试，输出到终端，不影响程序架构
- **覆盖范围**：
  - VAD 处理时延（websocket 收到音频 → vad.process() 返回）
  - LLM 首 Token 时延（TTFT）
  - LLM 总生成时延
  - TTS 首包时延
  - TTS 总合成时延
  - 端到端时延（首音频 / 完成）
  - 中断处理时延
- **日志格式**：
  ```
  [TurnTiming] Enc: 编码时延 | Chk: LLM Check时延 | ASR: 识别时延 | St: 状态预测时延 | Total: 总时延 | Text: 识别文本
  ```

### 4. ASR 模型选型
- **问题**：Paraformer 识别不准确、出现幻觉、模型异常退出
- **过程**：Paraformer → Whisper → SenseVoice
- **最终方案**：使用 SenseVoice（`config.yaml` 中 `model_name: sensevoice`）
- **补充**：SenseVoice 支持多语言自动检测（`<|zh|>`、`<|en|>`、`<|ja|>` 等）

### 5. WebSocket 断连问题
- **问题**：没有操作时 WebSocket 突然断开
- **原因分析**：
  - VAD 客户端超时时间过短（1.0s），服务端处理稍慢就触发异常
  - 异常处理中旧连接未清理，形成"孤儿连接"，服务端 session 混乱
  - VAD 服务器 WebSocket 断开后，异常传播导致前端连接断开
- **解决**：
  - `vad_client.py` 超时时间改为 10.0s
  - 优化异常处理逻辑
- **关键日志**：
  ```
  RuntimeError: Cannot call "receive" once a disconnect message has been received.
  [TurnTaking] websocket disconnected
  ```

### 6. 识别时延增长问题
- **问题**：实际体感文字识别越来越延后，日志显示每 chunk 只要 80ms 但整体越来越慢
- **原因分析**：
  - **KV cache 累积**：每 chunk 的 key/value 缓存不断增长，注意力计算 O(n²)，越长越慢
  - **cascade_buffer 累积**：ASR 每次处理整个 buffer（最多 3.2s），buffer 未满前一直在增长
- **尝试的方案**：
  - KV cache 滑动窗口（限制 800 token）—— 效果不明显（阈值太高，正常一句话达不到）
  - cascade_buffer 改小（3.2s → 1.6s/2.4s/2.0s）—— 效果不明显
  - 最终改回 3.2s
- **结论**：
  - 3.2s 之后 cascade_buffer 封顶，ASR 耗时趋于稳定
  - 静音期间音频也在往 cascade_buffer 累加
  - Enc 变慢可能与 CUDA 内存碎片化有关（两个模型共享 GPU）

### 7. chunk_size 调优
- **问题**：每 chunk 处理频率过高，GPU 负载大
- **解决**：`chunk_size: 2560`（160ms）→ `5120`（320ms）
- **效果**：每秒处理次数减半（6.25 → 3.1 次/秒），GPU 总负载下降
- **影响**：VAD End 响应最多延迟 160ms；VAD 判断质量、ASR 准确率、前端展示均不受影响

### 8. VAD 幻觉问题
- **问题**：VAD 识别了不存在的音频（LLM Check 通过但无实际语音）
- **分析**：`LLM Check: No speech detected` 之后 State 仍预测为 `<|user_nonidle|>` 是正常的模型行为，不产生实际回复，无需处理

### 9. 程序崩溃问题
- **问题**：程序最后莫名其妙崩溃
- **解决**：通过日志分析发现与 WebSocket 断连、显存占用有关，多轮排查后稳定运行

### 10. 显存实时监测
- **问题**：需要实时监测 GPU 显存
- **解决**：添加了 [GPU] 实时显存监控日志，通过 nvidia-smi 辅助确认

### 11. 对话提示词（中文）
- **问题**：系统提示词是英文，需要改为中文
- **解决**：`llm_client.py` 中三个类（QwenLLM_stream / QwenLLM_IndexTTS_stream / QwenLLM_Cosyvoice_stream）的 SYSTEM_PROMPT 全部改为中文
- **注意**：修改提示词需三处同步修改

### 12. 自我介绍过长
- **问题**：LLM 自我介绍太长
- **建议**：在 SYSTEM_PROMPT 末尾加"回答尽量简短，自我介绍不超过3句话"

### 13. VAD Process 与 VAD End 时延区别
- **问题**：app.py 的 VAD Process 时延与 Dialogue 的 VAD End 时延不一致
- **解释**：
  - `VAD Process: 0.355s` = Dialogue → VAD 服务器网络往返耗时（单 chunk）
  - `[TurnTiming] VAD End: 4.804s` = VAD Start 到 VAD End 总时长（整个说话轮次）

### 14. TTS 启动失败（tokenizer 报错）
- **问题**：`TypeError: expected str, bytes or os.PathLike object, not NoneType`
- **原因**：模型 `tokenizer_config.json` 只有 26 字节（只有 model_max_length），transformers 新版本（4.57.1）加载 GPT2 tokenizer 需要完整配置
- **解决**：降级 `transformers==4.56.1` 或使用绝对路径启动

### 15. 启动命令路径问题
- **问题**：`--model_dir` 相对路径层级错误
- **解决**：`../../../../` → `../../../`（从 `modules/index_tts_vllm/` 向上 3 级到项目根目录）
- **注意**：TTS 环境 `.venv-tts`（Python 3.12）、主环境 `.venv`（Python 3.10.16）

---

## 二、关键文件改动清单

| 文件 | 修改内容 |
|------|---------|
| `config/config.yaml` | `chunk_size: 5120`（原2560）；ASR 模型改为 sensevoice |
| `dialogue_system/app.py` | 时延计时、日志输出（LLM TTFT、TTS 首包、端到端时延） |
| `dialogue_system/clients/llm_client.py` | SYSTEM_PROMPT 改为中文 |
| `dialogue_system/clients/vad_client.py` | timeout: 1.0 → 10.0 |
| `service/model.py` | cascade_buffer 限制、时延计时（Enc/Chk/ASR/St） |
| `model/asr.py` | SenseVoice 语种检测日志（`[SensevoiceLang]`） |

---

## 三、部署文档编写

- 创建了 `部署文档.txt`，后转为 `部署文档.md`
- 内容：系统架构概述、环境配置（uv 创建环境）、模型下载、一键启动、双卡运行方案（GPU0: VAD+TTS，GPU1: LLM）、服务详情、故障排除、性能调优、时延参考指标
- 模型来源：git clone `https://github.com/Soul-AILab/SoulX-Duplug.git`，切换到 dialogue-system 分支
- 服务器：ubuntu (IP: 172.88.88.14)，CUDA 12.4

---

## 四、当前系统配置

### 双卡部署
- **GPU 0**：VAD Server (8000) + TTS Server (6006)
- **GPU 1**：LLM Server (6007)
- **无 GPU**：Dialogue System (55556)

### 启动命令（完整）
```bash
# 1. TTS Server (GPU 0)
CUDA_VISIBLE_DEVICES=0 source /nfs/kubeflow-data/SoulX/SoulX-Duplug/.venv-tts/bin/activate
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug/dialogue_system/modules/index_tts_vllm
python api_server.py --host 0.0.0.0 --port 6006 --model_dir /nfs/kubeflow-data/SoulX/SoulX-Duplug/pretrained_models/Index-TTS-1.5-vLLM --gpu_memory_utilization 0.35

# 2. LLM Server (GPU 1)
source /nfs/kubeflow-data/SoulX/SoulX-Duplug/.venv/bin/activate
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug/dialogue_system/modules/qwen_llm
python llm_server.py --host 0.0.0.0 --port 6007 --model_dir /nfs/kubeflow-data/SoulX/SoulX-Duplug/pretrained_models/Qwen2.5-7B-Instruct --device cuda:1

# 3. VAD Server (GPU 0)
CUDA_VISIBLE_DEVICES=0 source /nfs/kubeflow-data/SoulX/SoulX-Duplug/.venv/bin/activate
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug
uvicorn server:app --host 0.0.0.0 --port 8000 --workers 1

# 4. Dialogue System
source /nfs/kubeflow-data/SoulX/SoulX-Duplug/.venv/bin/activate
cd /nfs/kubeflow-data/SoulX/SoulX-Duplug/dialogue_system
python app.py
```

---

## 五、时延参考指标（正常范围）

| 指标 | 正常范围 |
|------|---------|
| Enc（编码） | 0.01-0.03s |
| Chk（LLM Check） | 0.03-0.06s |
| ASR（语音识别） | 0.06-0.12s |
| St（状态预测） | 0.03-0.06s |
| Total（总时延） | 0.15-0.30s |
| LLM 首 Token | 0.3-1.0s |
| 端到端（首音频） | 0.5-1.5s |
