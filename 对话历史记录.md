# SoulX-Duplug 对话历史记录

> 记录时间：2026-07-27
> 项目路径：`/nfs/kubeflow-data/SoulX/SoulX-Duplug`
> 说明：本文件汇总了与 AI 助手就本项目（Turn-Taking 语音对话系统）进行的所有对话历史，包括早期会话（原始上下文被压缩，依据会话摘要整理）与本次会话（原文记录）。

---

## 一、项目背景

SoulX-Duplug 是一个 **Turn-Taking（轮流说话）语音对话系统**，核心组件：

- `service/model.py`：Turn-Taking 核心推理逻辑（语音活动检测 → 状态预测 → 级联 ASR）
- `service/session.py`：会话状态管理（音频缓存、状态跟踪、生命周期）
- `server.py`：Turn-Taking 服务入口（FastAPI + WebSocket `/turn` 端点，加载模型、管理会话、GC 回收超时会话）
- `dialogue_system/app.py`：对话系统主服务（WebSocket 端点、VAD 池、LLM/TTS 调用、PipelineTiming 时延打印）
- `dialogue_system/clients/vad_client.py`：音频转发客户端（与 server.py 建立 WebSocket 连接）
- `dialogue_system/frontend/script.js`：前端页面（音频采集、WebSocket 通信、TTS 播放）
- `config/config.yaml`：配置文件（ASR 模型类型、模型路径等）

**关键技术点**：
- **级联 ASR（Cascade ASR）**：先通过 LLM 快速检测是否有语音活动，若有则调用外部 ASR 模型（Whisper/SenseVoice）进行精确文本识别。
- **Turn-Taking 模型**：检测用户语音状态（idle/nonidle/speak），控制对话轮次切换。
- **WebSocket 通信**：客户端 ↔ server.py 之间传输音频与处理结果。

---

## 二、已解决的问题总览

### 1. 时延测试输出（终端打印，带识别文字）

**原始需求**：在 `service/model.py` 中添加时延测试输出，最初希望输出到浏览器 F12 控制台，后来改为在**终端**输出，且时延中需带有识别出的文字。

**实现方案**（`service/model.py` 中已落地）：
- 各关键步骤计时：音频编码+embed（`Enc`）、LLM check（语音活动检测）、级联 ASR、状态预测（State pred）、总 chunk 时延。
- VAD Start / VAD End 计时：记录一次语音活动从开始到结束的时长，输出语言与识别文字。
- 每帧处理后终端打印类似：
  ```
  [TurnTiming] Enc: 0.1234s | LLM check: 0.0567s | ASR: 0.2345s | State pred: 0.0789s | ...
  [TurnTiming] VAD End (cascade): 1.234s | Lang: zh | Text: 识别出的文字
  ```

**语音处理链路**（用户提问梳理）：
```
麦克风音频 → WebSocket 转发到 server.py(/turn)
  → TurnSession.feed_audio()
    → 语音转 token（audio_to_tokens）
    → token 转 embedding（tokens_to_embeds）
    → LLM check：判断是否有语音活动
    → 有语音活动 → 级联 ASR：调用 Whisper/SenseVoice 精确识别文字
    → 状态预测（state_predict）→ 输出状态 idle / nonidle / speak
  → 状态为 speak → 对话系统（app.py）送入 Qwen7B/LLM 生成回复
  → TTS 合成音频 → 回传前端播放
```

### 2. ASR 模型切换 Whisper

**原始需求**：将 ASR 模型从 SenseVoice 换成 Whisper。

**方案**：修改 `config/config.yaml`，`asr.model_name` 从 `sensevoice` 改为 `whisper`，并支持本地模型路径 `pretrained_models/whisper-large-v3-turbo` 加载，避免从网络下载。

**注意**：当前 `config/config.yaml` 实际仍为 `sensevoice`（见"四、当前代码实际状态"），如需使用 Whisper 需再次修改该配置项。

### 3. WebSocket 连接稳定性（超时 / 异常中断 / 崩溃）

**症状**：
- `WebSocketTimeoutException: Connection timed out`
- `'NoneType' object is not subscriptable`
- `'NoneType' object has no attribute 'send'`
- server.py 崩溃、客户端无限重连刷屏

**原因分析**：
- `vad_client.py` 中 `timeout` 过短（1.0 秒）——服务器处理一帧音频（语音转 token → embedding → ASR → 状态预测）经常超过 1 秒，导致 `recv()` 超时抛异常。
- 使用 `bare except:` 捕获一切异常并重连，且重连后再次 `send/recv` 若仍超时会再次进入 `except`，形成**无限重连循环**，永不复原。
- `server.py` 处理消息时未校验字段，非法消息（缺 `session_id`/`audio`）导致 `'NoneType' object is not subscriptable`。
- `self.ws` 可能为 `None`（未连接或连接已关闭）时直接调用 `.send()`，导致 `'NoneType' object has no attribute 'send'`。

**解决方案（讨论得出）**：
- `timeout` 调整为 10.0 秒，给服务器足够处理时间。
- 只对特定异常（超时/连接关闭/AttributeError）做有限次重连（如 1 次），重试失败则跳过当前帧，避免无限循环。
- `process()` 开头检查 `self.ws is None`，为空先 `connect()`。
- `server.py` 中对消息增加字段校验、对音频解码加 `try/except`、外层 `try/except` 保证单条消息异常不关闭连接。

**注意**：当前 `vad_client.py` 实际代码仍为 `bare except:`（重连一次），尚未落实"限定异常类型重连 + ws None 检查"的改进（见"四、当前代码实际状态"）。

### 4. 各类报错 / 日志解读（已向用户解释）

| 报错/日志 | 原因 | 处理方法 |
|---|---|---|
| `Unknown event: turn_timing` | 浏览器缓存了旧版 `script.js`，未加载到移除 `turn_timing` 事件处理的新版本 | `Ctrl + Shift + R` 硬刷新页面强制加载新文件 |
| `load_state_dict failed: Unexpected key(s) in state_dict: "embed_tokens_func.weight"` | 模型中动态赋值的 `embed_tokens_func.weight` 不属于静态 `state_dict` | 系统自动 `Retry with strict=False` 忽略不匹配键，**无害，无需处理** |
| `WebSocketTimeoutException: Connection timed out` | `vad_client.py` timeout 设置过长/过短 + server.py 处理非法消息抛异常导致连接关闭 | 调整 timeout、加字段校验、异常捕获 |
| `'NoneType' object is not subscriptable` | 未校验 `data` 是否为 None / 缺 `session_id`/`audio` 字段 | 添加字段校验，非法消息直接跳过 |
| `'NoneType' object has no attribute 'send'` | `self.ws` 为 None（未连接/连接已关闭）时调用 `.send()` | 增加 `ws is None` 检查，先重连 |
| `favicon.ico:1 GET ... 404 (Not Found)` | 页面请求站点图标，服务器未提供 | **正常现象，可忽略**（或添加 favicon） |
| `GET /.well-known/appspecific/com.chrome.devtools.json 404` | Chrome 开发者工具自动请求调试配置文件 | **正常现象，可忽略**，不影响功能 |
| `Terminal#963-1000` 中 `^C` | 手动 Ctrl+C 停止服务 | 非错误；重启后确保 `server.py` 与 `app.py` 都重启 |

### 5. stop 信号机制梳理（用户提问：现在的 stop 信号用什么发送）

核心是 `ChatSession.interrupt()`（`dialogue_system/app.py`）：
```python
def interrupt(self):
    self._stop_event.set()                                    # ① 线程级停止
    emit_to_room(self.client_id, "stop_audio", ...)           # ② 前端停止播放
    emit_to_room(self.client_id, "circle_status", ...)
```

**触发时机（3 处调用 interrupt()）**：
| 触发场景 | 位置 |
|---|---|
| 打断（barge-in）：AI 说话时用户出声，VAD 返回 `[None]` | app.py VAD 处理逻辑 |
| 新一轮对话开始前 | pipeline_worker |
| 客户端手动停止 | 前端发 JSON `{"event": "duplex_stop"}` |

**两个 stop 通道**：
- `threading.Event`（`session._stop_event`）：停止后端 LLM 生成和 TTS 合成线程（传给 `llm.generate_with_history(stop_event=...)`）。
- `stop_audio` WebSocket 事件：经 `emit_to_room` 发 JSON 帧给浏览器，前端 `script.js` 收到后 abort TTS 播放；同时转发到远端播放器（WebSocket relay 模式）停止播放。

**UDP 远端模式注意**：`emit_to_room` 对 UDP 远端**不再发送 stop_audio 控制事件**，远端依赖数据流中断自然停播。

---

## 三、完整对话历史

### 阶段一：早期会话（原始上下文已压缩，据会话摘要整理）

**用户 1**：`/nfs/kubeflow-data/SoulX/SoulX-Duplug/service/model.py` 看一下这个文件，我想要加一些时延测试的输出，我想要在原页面的 f12 输出各个步骤的时延。

**助手**：阅读 `model.py`，在语音处理关键步骤（audio_to_tokens / tokens_to_embeds / llm_check / cascade_asr / asr / state_predict 等）添加时间戳计时，构造 `timing` 字典。

**用户 2**：一段语音输入之后经过处理的链路是怎样的？

**助手**：梳理全链路：麦克风音频 → WebSocket → server.py `/turn` → TurnSession.feed_audio → 语音转 token → token 转 embedding → LLM 检测语音活动 → 级联 ASR 识别 → 状态预测（idle/nonidle/speak）→ speak 时 app.py 调 LLM 生成回复 → TTS 合成 → 回传前端播放。

**用户 3**：state_predict 之后处于 speak 状态的话就送入 qwen7b 吗？

**助手**：确认：状态预测为 speak 后，对话系统将音频识别文本送入 LLM（如 Qwen7B）生成回复，随后进入 TTS 合成与播放，同时停止 TTS 播放等待用户下一轮。

**用户 4**：生成回复和送入 7b 模型的时延也要测。

**助手**：在 app.py 的 LLM/TTS 调用链路添加时延计时，覆盖"语音输入 → 回复生成 → TTS 输出"全链路。

**用户 5**：贴出 F12 控制台日志 `Unknown event: turn_timing`、`Received stop_audio signal` 等，问为什么输出这些。

**助手**：解释：`Unknown event: turn_timing` 是因为浏览器缓存旧版 `script.js`，新版本已移除 `turn_timing` 事件处理；建议 `Ctrl + Shift + R` 硬刷新。`Received stop_audio signal` 是前端收到服务器的停止播放信号。

**用户 6**：那不要在 f12 输出了，在终端输出吧，我希望时延里面带有识别出的文字。

**助手**：改造实现：移除前端输出，改为在**终端**打印时延，格式类似 `[Timing] state=... | infer=... (audio2tok=... asr=... state_pred=...) | text="识别文字"`。

**用户 7**：贴出更多日志，问"这些是什么"。

**助手**：继续逐条解读日志含义（详见上文"报错解读"表）。

**用户 8**：`Terminal#15-18` 这是什么？

**助手**：解读该终端片段（模型加载 / ASR 下载 / WebSocket 连接等）。

**用户 9**：`Terminal#31-139` 都是什么意思？

**助手**：解读该段终端日志（funasr 版本、SenseVoice 模型下载、模型加载成功等）。

**用户 10**：什么是 cascade ASR？

**助手**：解释级联 ASR：先由轻量 LLM 快速判断是否有语音活动；检测到语音活动后，再调用外部精确 ASR 模型（Whisper/SenseVoice）识别文字。好处是省算力、响应快，精确识别只在实际说话时触发。

**用户 11-13**：`Terminal#1-140` / `Terminal#1007-1019` / `Terminal#160-273` 都是什么意思？

**助手**：分段解读终端日志（模型加载过程、推理过程、时延打印等）。

**用户 14**：`favicon.ico:1 GET http://localhost:55556/favicon.ico 404 (Not Found)`

**助手**：解释为页面图标请求 404，正常现象，不影响功能。

**用户 15**：`Terminal#437-574` 为什么会这样阿？

**助手**：解读该段日志中出现的异常（超时/连接断开等），分析根因。

**用户 16**：什么情况下会 Received stop_audio signal？

**助手**：解释 stop_audio 的触发时机（见上文"stop 信号机制"）：AI 说话时用户打断（barge-in）、新一轮对话开始前、客户端手动 duplex_stop。

**用户 17**：`Terminal#612-707` 这个 bug 到底什么原因？

**助手**：定位到 `vad_client.py` 的异常重连逻辑问题（bare except 无限循环）与 `server.py` 消息处理问题。

**用户 18**：`Terminal#1003-1020` server.py 为什么会崩溃？

**助手**：分析 server.py 崩溃原因：处理非法消息（缺字段）导致 `'NoneType' object is not subscriptable`；WebSocket 异常未妥善处理导致连接/服务异常。给出修复方向：字段校验、try/while 结构调整、异常捕获。

**用户 19**：可是我都重启好多次了。

**助手**：指出重启不能解决问题的原因：问题出在代码逻辑（bare except 无限重连 + timeout 配置 + 字段校验缺失），而非运行时状态；需要修改代码后重启才有效。并检查 `vad_client.py` 的 `server_url` 端口（`ws://localhost:8000/turn`）与 `server.py` 实际监听端口（uvicorn `--port 8000`）是否一致。

---

### 阶段二：本次会话（原文记录）

**用户 20**：选中终端 `Terminal#992-1009`，内容为大量 `[VadClient] unexpected error: 'NoneType' object has no attribute 'send'`，问"一直发这个"。

**助手**：说明修复两处：
1. `process()` 开头检查 `self.ws is None`，为空先 `connect()`；
2. 将 `AttributeError` 加入重连异常列表（`'NoneType' object has no attribute 'send'` 本质是 `AttributeError`），触发重连而不是直接跳过。
建议重启 `app.py` 和 `server.py` 验证。

**用户 21**：选中终端 `Terminal#963-1000`，内容为 server.py 的启动、模型加载、SenseVoice 下载、websocket 连接/断开、`^C` 停止、重新启动等。

**助手**：逐段解读：
- 前段显示 ASR 仍是 SenseVoice（`Downloading Model from modelscope ... SenseVoiceSmall`）——配置未生效为 whisper；
- `websocket connected` / `disconnected`：客户端连上又断开（可能是遗留的 app.py）；
- `^C`：手动停止服务，非错误；
- 后续为重新启动 server.py，正在加载模型。
建议：`server.py` 与 `app.py` 两个服务都重启，确保 ASR 用 whisper、连接不断。

**用户 22**：选中终端 `Terminal#1010-1011`，内容为 `GET /.well-known/appspecific/com.chrome.devtools.json 404 Not Found`。

**助手**：解释为 Chrome 开发者工具自动请求调试配置文件，服务器无此端点返回 404，**完全正常，可忽略**。

**用户 23**：现在的 stop 信号是用什么发送的？

**助手**：完整讲解 stop 信号机制（详见上文"stop 信号机制梳理"）：服务器 `ChatSession.interrupt()` 统一发送——内部 `threading.Event` 停 LLM/TTS 线程，外部 WebSocket `stop_audio` 事件停前端/远端播放；三条触发路径（barge-in、新一轮开始、duplex_stop）。

**用户 24（当前）**：整理一下对话历史，告诉我我都解决了什么问题，把所有对话历史存储到一个文件里，全部放进去。

**助手**：生成本文件 `对话历史记录.md`，汇总全部对话与已解决问题。

---

## 四、当前代码实际状态（2026-07-27 核对）

| 文件 | 实际状态 |
|---|---|
| `service/model.py` | 已含 `[TurnTiming]` / `[Timing]` 时延打印（VAD Start/End、Enc、LLM check、Cascade ASR、State pred、总 chunk 时延）；含 CloudSTT 云端识别（失败回退本地） |
| `dialogue_system/clients/vad_client.py` | `timeout=10.0`；`process()` 中为 **`bare except:`**（异常时 `connect()` 重连并重发一次，失败则抛给上层）；**尚无** `ws is None` 前置检查、**尚无** 按异常类型限次重连 |
| `server.py` | 外层 `try/except` 保证单条消息异常不关闭连接；音频解码有 `try/except`；字段校验**未显式存在**（`data["session_id"]` 直接取值），消息类型非 audio 直接 continue |
| `config/config.yaml` | `asr.model_name: sensevoice`（**当前仍是 sensevoice**，若用 whisper 需改为 `whisper` 并配置 `whisper_model` 路径） |

---

## 五、待办 / 注意项

1. **vad_client.py 优化未落实**：会话中讨论的"限定异常类型（WebSocketTimeoutException / WebSocketConnectionClosedException / AttributeError）重连一次，其他异常跳过当前帧；`ws is None` 时先 connect"尚未写入文件。若 `'NoneType' object has no attribute 'send'` 刷屏问题仍在，需应用该改动。
2. **ASR 切换 Whisper 未生效**：`config.yaml` 当前为 `sensevoice`，需改为 `whisper` 并确保本地模型路径存在（`pretrained_models/whisper-large-v3-turbo`），同时确认 ASR 加载代码支持 whisper 分支。
3. **重启验证**：修改代码后需同时重启 `server.py`（uvicorn，端口 8000）与 `app.py`，并硬刷新浏览器页面（Ctrl+Shift+R）。
4. **端口一致性**：`vad_client.py` 连接 `ws://localhost:8000/turn`，需确保 server.py 监听同一端口。
