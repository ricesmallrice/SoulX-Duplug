SoulX-Duplug 全双工语音对话系统部署文档
=============================================

本程序基于开源项目 SoulX-Duplug，克隆自：
git clone https://github.com/Soul-AILab/SoulX-Duplug.git
并切换到 dialogue-system 分支。

系统架构概述
-------------
本系统集成四个核心Web服务，实现全双工实时语音对话交互：

- VAD Server  (端口8000):  语音活动检测与状态预测服务（SoulX-Duplug）
- LLM Server  (端口6007):  大语言模型生成服务（Qwen2.5-7B-Instruct）
- TTS Server  (端口6006):  语音合成服务（IndexTTS-vLLM）
- Dialogue System (端口55556): Web前端与对话管理服务



服务器信息
- 服务器: ubuntu (IP: 172.88.88.14)
- 用户: hjadmin
- CUDA版本: 12.4
- 虚拟环境: SoulX-Duplug/.venv（Python 3.10.16，VAD/LLM/Dialogue共用）
- 虚拟环境: SoulX-Duplug/.venv-tts（Python 3.12，TTS服务独立环境）
- 项目目录: SoulX-Duplug

环境配置
---------
模型文件位置（下载到项目根目录下的 pretrained_models/ 文件夹）
- SoulX-Duplug模型:   pretrained_models/SoulX-Duplug/SoulX-Duplug-0.6B-Bilingual.pth
- Qwen3-0.6B:         pretrained_models/Qwen3-0.6B-expand_vocab_v2
- GLM语音Tokenizer:   pretrained_models/glm-4-voice-tokenizer
- Qwen2.5-7B:         pretrained_models/Qwen2.5-7B-Instruct
- IndexTTS-vLLM:      pretrained_models/Index-TTS-1.5-vLLM

环境准备
---------
本系统依赖两个独立的Python环境：

1. 主环境（VAD / LLM / Dialogue System）
```bash
# 在 SoulX-Duplug 项目根目录下执行
uv venv --python 3.10.16 --seed
source .venv/bin/activate
pip install -r requirements.txt
```

2. TTS环境（IndexTTS-vLLM）
```bash
# 在 SoulX-Duplug 项目根目录下执行
uv venv .venv-tts --python 3.12 --seed
source .venv-tts/bin/activate
pip install -r dialogue_system/modules/index_tts_vllm/requirements.txt
```

**模型下载**（国内用户推荐使用 modelscope）

1. SoulX-Duplug 模型（含Qwen3-0.6B + GLM tokenizer + LoRA权重）
```bash
# 从 HuggingFace 下载
huggingface-cli download --resume-download Soul-AILab/SoulX-Duplug-0.6B --local-dir ../pretrained_models

# 国内用户使用 modelscope
modelscope download --model Soul-AILab/SoulX-Duplug-0.6B --local_dir ../pretrained_models
```
下载后模型文件结构:
```text
pretrained_models/
├── Qwen3-0.6B-expand_vocab_v2/
├── glm-4-voice-tokenizer/
└── SoulX-Duplug/
    └── SoulX-Duplug-0.6B-Bilingual.pth
```

2. Qwen2.5-7B-Instruct（LLM对话模型）
```bash
modelscope download --model Qwen/Qwen2.5-7B-Instruct --local_dir ../pretrained_models/Qwen2.5-7B-Instruct
```

3. Index-TTS-1.5-vLLM（语音合成模型）
```bash
modelscope download --model kusuriuri/Index-TTS-1.5-vLLM --local_dir ../pretrained_models/Index-TTS-1.5-vLLM
```


一键启动所有服务（按顺序，需四个终端）
-----------------
```bash
# TTS Server（GPU 0）
source SoulX-Duplug/.venv-tts/bin/activate
cd SoulX-Duplug/dialogue_system/modules/index_tts_vllm
CUDA_VISIBLE_DEVICES=0 python api_server.py --host 0.0.0.0 --port 6006 --model_dir ../../../pretrained_models/Index-TTS-1.5-vLLM --gpu_memory_utilization 0.35

# LLM Server（GPU 1）
source SoulX-Duplug/.venv/bin/activate
cd SoulX-Duplug/dialogue_system/modules/qwen_llm
CUDA_VISIBLE_DEVICES=1 python llm_server.py --host 0.0.0.0 --port 6007 --model_dir ../../../pretrained_models/Qwen2.5-7B-Instruct

# VAD Server（GPU 0）
source SoulX-Duplug/.venv/bin/activate
cd SoulX-Duplug
CUDA_VISIBLE_DEVICES=0 uvicorn server:app --host 0.0.0.0 --port 8000 --workers 1

# Dialogue System（无GPU需求）
source SoulX-Duplug/.venv/bin/activate
cd SoulX-Duplug/dialogue_system
python app.py
```


服务详情与访问地址
-------------------
1. VAD Server - 语音活动检测服务 (端口: 8000)

核心功能:
- 音频流语音活动检测（VAD）
- LLM Check：判断语音片段是否包含有效人声
- Cascade ASR：流式语音识别（SenseVoice/Paraformer/Whisper）
- State Prediction：预测用户状态（说话中/空闲/打断）
- 支持全双工实时音频处理

配置文件: SoulX-Duplug/config/config.yaml
重要配置项:
- chunk_size: 每帧音频采样数（5120 = 320ms）
- audio_back_size: 向后上下文窗口（15360 = 960ms）
- audio_ahead_size: 向前上下文窗口（640 = 40ms）
ASR模型说明:
- sensevoice: 多语言识别，支持自动语种检测（当前使用）
- paraformer: 中文识别优化
- whisper: 多语言识别（可选large模型）
- max_wait_num: 最大等待帧数（8帧后强制判断）
- max_mistake_num: 连续"无语音"次数阈值（3次后判定语音结束）
- far_field_threshold: 远场检测阈值


2. LLM Server - 大语言模型服务 (端口: 6007)

核心功能:
- 基于Qwen2.5-7B-Instruct的文本生成
- 支持流式输出（Server-Sent Events）
- 集成TTS客户端，支持LLM+TTS联动
- 支持对话历史管理

API接口:
```bash
# LLM文本生成
POST http://localhost:6007/chat
Body: { "messages": [{"role": "system", "content": "..."}, {"role": "user", "content": "..."}] }
Response: 流式文本（text/event-stream）

# LLM+TTS联合生成
POST http://localhost:6007/chat_indextts
Response: 二进制流（文本标记+音频数据包）
```

对话提示词配置:
文件: SoulX-Duplug/dialogue_system/clients/llm_client.py
类: QwenLLM_stream, QwenLLM_IndexTTS_stream, QwenLLM_Cosyvoice_stream
- 三个类的SYSTEM_PROMPT内容一致，分别对应纯文本、IndexTTS后端和CosyVoice后端
- 修改提示词需三处同步修改


3. TTS Server - 语音合成服务 (端口: 6006)

核心功能:
- 文本到语音合成
- 流式音频生成

4. Dialogue System - 对话系统前端 (端口: 55556)

访问地址: http://localhost:55556

前端文件:
- frontend/index.html: 页面结构
- frontend/script.js: 前端逻辑（WebSocket音频传输）
- frontend/processor.js: 音频处理
- frontend/style.css: 样式

核心功能:
- 实时语音采集与播放（Web Audio API）
- WebSocket双向音频流传输
- 对话状态展示与交互
- 中断检测与响应





日志监控


一、Dialogue System 终端输出（TurnTiming 时延监控）

| 字段 | 含义 |
|---|---|
| VAD Start / VAD End | 语音活动检测（Voice Activity Detection）的开始/结束时间点 |
| Enc | Encoder 编码时延，即语音特征提取耗时 |
| Chk | Check 时延，即 LLM 检查/推理时延 |
| ASR | 语音识别时延（Automatic Speech Recognition），语音→文本识别耗时 |
| St | State 状态预测时延（意图识别或状态预测模块） |
| Total | 该轮从 VAD End 到处理完成的总时延 |
| No speech | VAD 检测到无语音，该轮不产生有效文本 |
| SensevoiceLang | 语种检测结果，<\|zh\|>=中文，<\|en\|>=英文，<\|ko\|>=韩文 |
| Text | 识别出的文本内容 |


二、App 日志（E2E 流程监控）

| 字段 | 含义 |
|---|---|
| VAD Process | VAD 处理耗时（从音频中截取有效语音段的时间） |
| Utterance | VAD 切分出的用户语音片段文本 |
| ASR result | 语音识别最终输出的文本结果 |
| LLM Chunk | LLM 返回的一个流式文本片段（streaming chunk） |
| LLM TTFT | Time To First Token — 从请求发出到收到 LLM 第一个 token 的耗时 |
| TTS First Audio | 从 TTS 开始到产出第一帧音频的耗时 |
| E2E First Audio | 端到端首帧音频时延（从用户说完到 AI 发出第一个声音的总耗时） |
| Interrupt Latency (TTS loop) | TTS 播放循环中检测到打断并停止播放的延迟 |
| Interrupt Latency (post-TTS) | TTS 停止后后续清理/切换状态的额外延迟 |
| LLM Total | LLM 整个请求的总耗时（从发送到所有 chunk 接收完毕） |
| TTS Total | TTS 整个合成的总耗时（从开始到生成完所有音频） |
| E2E Complete | 端到端完整一轮总耗时（从用户说话结束到 AI 完整回复结束） |
| Interrupted mid-stream | 本轮被用户打断。Ratio 表示已播放比例（如0.30=只播了30%被打断），Truncated message 为被截断的文本 |
